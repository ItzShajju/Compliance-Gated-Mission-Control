# Compliance-Gated Mission Control — Polished Streamlit Prototype

This repository contains a polished Streamlit prototype demonstrating the Compliance-Gated Mission Control dashboard (Mission Control, Engineering Analytics, Compliance Gates). The app includes embedded realistic sample data and produces downloadable evidence bundles with PDF + CSV + JSON.

Files:
- app.py
- requirements.txt
- README_DEPLOY.md

Deploy (Streamlit Community Cloud)
1. Create a GitHub repo and push these files.
2. Go to https://share.streamlit.io/ and sign in with GitHub.
3. Create a new app, select your repo/branch and `app.py`, then deploy.
4. Share the generated URL with leadership.

Local run (if python available)
pip install -r requirements.txt
streamlit run app.py